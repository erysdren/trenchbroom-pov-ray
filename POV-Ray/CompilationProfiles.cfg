{
	"profiles": [
		{
			"name": "Render",
			"tasks": [
				{
					"parameters": "${WORK_DIR_PATH}/${MAP_BASE_NAME}.map ${WORK_DIR_PATH}/${MAP_BASE_NAME}.pov ${WORK_DIR_PATH}/${MAP_BASE_NAME}.ini",
					"tool": "${tb2pov}",
					"type": "tool"
				},
				{
					"parameters": "${WORK_DIR_PATH}/${MAP_BASE_NAME}.ini",
					"tool": "${povray}",
					"type": "tool"
				}
			],
			"workdir": "${MAP_DIR_PATH}"
		}
	],
	"version": 1
}
